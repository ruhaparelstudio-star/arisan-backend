# ---- Client-safe ----
NEXT_PUBLIC_APP_NAME=Arisan Admin

# ---- Server-side only (JANGAN prefix NEXT_PUBLIC_) ----
# Dipakai oleh app/api/proxy/* saat integrasi data live ke arisan-api diaktifkan.
# Saat ini dashboard berjalan dengan data mock (lihat lib/data.ts), belum butuh API.
ADMIN_SECRET_KEY=
API_URL=http://api:3001
